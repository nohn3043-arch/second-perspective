# SPL-G1 与 Materica 因果映射：从 RTL 到材料

## 问题背景

SPL-G1 的 Materica-specification 定义了四项物理层因果映射要求——任何能承载 G1 逻辑的物理材料必须满足二元相位约束、定向信号、存算近接性、安全边界不可逆。但此前 RTL 与规范之间存在真空地带：没有硬件层面去主动验证运行它的材料是否真的满足这些物理约束。

## 改造方案：硬件级材料合规抽象层

新增独立模块 `materica_compliance_unit`，作为 G1 逻辑与物理材料之间的合规适配层：

- 上层：G1 逻辑永远不变（PIM + Sequencer + Causal Audit）
- 下层：物理传感器接口（材料变就只换这一层）

四项编码为四道硬件闸门，任一 FAIL → 芯片熔断。

### Materica #1: 二元相位约束 — 自发翻转监测

每个 cell 维持一个 16-bit 滑动窗口 `phase_stable_counter`。NOP 操作下状态不该变，变了就是"叙事噪声"的物理等价物。连续多次自发翻转 → 监测窗口跌破阈值 → violation。

仿真级额外检测 X/Z 态传播（合成时自动剥除）。

### Materica #2: 定向信号 — 多目标广播锁定 + 方向违规计数

各向同性扩散的数字等价形式是同一事务多目标 WRITE。检测到即粘性锁存（`multi_target_latched`，只有 rst_n 能清除）。同时计数 CONFIG 反向事务（source≠target 的 CONFIG），累计超过阈值即触发。

### Materica #3: PIM 近接性 — 地址恒等校验

核心不变式：在存算一体架构中，计算位置 == 数据存储位置，始终成立。逐单元比较 `compute_addr == store_addr`，不匹配即 violation（`LOAD_ROW`/`LOAD_COL` 邻居通信豁免——走定向总线，不算搬运）。

### Materica #4: SBC 不可逆 — 物理破坏锁存

三路物理传感器（温度/电压/辐射）OR 门 + 安全边界完整性检测。边界破坏一旦发生，锁存器 0→1 后不可恢复（软件无权限清除）。与现有 audit 熔断是 OR 关系——逻辑审计失败和物理材料违规各自独立触发芯片锁定。

## 验证结果

6/6 测试全部通过：全合规、相位自发翻转、多目标广播违规、存算地址分离、温度攻击、安全边界破坏不可逆。iverilog 12.0 编译零错误。

## 剩余工作

- 物理传感器接口从测试台模拟 → 真实片上传感器 IP
- Materica #2 枚举计数改为按违规类型分类
- #1 相位监测窗口按材料参数化（硅 vs DNA origami 不同窗口）
